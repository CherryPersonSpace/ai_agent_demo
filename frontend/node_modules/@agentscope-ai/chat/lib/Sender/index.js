var _excluded = ["styles", "classNames", "className", "rootClassName", "style", "defaultValue", "value", "readOnly", "enableFocusExpand", "sendDisabled", "allowEmptySubmit", "submitType", "onSubmit", "loading", "onCancel", "onChange", "onFocus", "onBlur", "actions", "actionAffix", "characterCountRender", "onKeyPress", "onKeyDown", "suggestions", "showCharacterCount", "disabled", "header", "onPaste", "allowSpeech", "onPasteFile", "onDropFile", "components"];
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : String(i); }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }
function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function _iterableToArrayLimit(r, l) { var t = null == r ? null : "undefined" != typeof Symbol && r[Symbol.iterator] || r["@@iterator"]; if (null != t) { var e, n, i, u, a = [], f = !0, o = !1; try { if (i = (t = t.call(r)).next, 0 === l) { if (Object(t) !== t) return; f = !1; } else for (; !(f = (e = i.call(t)).done) && (a.push(e.value), a.length !== l); f = !0); } catch (r) { o = !0, n = r; } finally { try { if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return; } finally { if (o) throw n; } } return a; } }
function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }
function _objectWithoutProperties(source, excluded) { if (source == null) return {}; var target = _objectWithoutPropertiesLoose(source, excluded); var key, i; if (Object.getOwnPropertySymbols) { var sourceSymbolKeys = Object.getOwnPropertySymbols(source); for (i = 0; i < sourceSymbolKeys.length; i++) { key = sourceSymbolKeys[i]; if (excluded.indexOf(key) >= 0) continue; if (!Object.prototype.propertyIsEnumerable.call(source, key)) continue; target[key] = source[key]; } } return target; }
function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }
import { Flex, Input } from 'antd';
import { Suggestion } from '@ant-design/x';
import classnames from 'classnames';
import { useMergedState } from 'rc-util';
import pickAttrs from 'rc-util/lib/pickAttrs';
import getValue from 'rc-util/lib/utils/get';
import React, { useState } from 'react';
import { useFocusWithin, useEventListener } from 'ahooks';
import useProxyImperativeHandle from "../Util/hooks/use-proxy-imperative-handle";
import { useProviderContext } from "./..";
import SenderHeader, { SendHeaderContext } from "./SenderHeader";
import { ActionButtonContext } from "./components/ActionButton";
import ClearButton from "./components/ClearButton";
import LoadingButton from "./components/LoadingButton";
import SendButton from "./components/SendButton";
import SpeechButton from "./components/SpeechButton";
import Style from "./style";
import useSpeech from "./useSpeech";
import ModeSelect from "./ModeSelect";
import BeforeUIContainer from "./BeforeUIContainer";
import { jsx as _jsx } from "react/jsx-runtime";
import { jsxs as _jsxs } from "react/jsx-runtime";
import { Fragment as _Fragment } from "react/jsx-runtime";
function hasFileTransfer(dataTransfer) {
  if (!dataTransfer) {
    return false;
  }
  return Array.from(dataTransfer.types || []).includes('Files');
}
function getFilesFromDataTransfer(dataTransfer) {
  if (!dataTransfer) {
    return [];
  }
  var files = Array.from(dataTransfer.files || []);
  if (files.length > 0) {
    return files;
  }
  return Array.from(dataTransfer.items || []).filter(function (item) {
    return item.kind === 'file';
  }).map(function (item) {
    return item.getAsFile();
  }).filter(function (file) {
    return file !== null;
  });
}
function getComponent(components, path, defaultComponent) {
  return getValue(components, path) || defaultComponent;
}
function getSlashCommandKeyword(inputValue) {
  if (!inputValue.startsWith('/')) {
    return null;
  }
  var contentAfterSlash = inputValue.slice(1);
  if (!contentAfterSlash) {
    return '';
  }

  // Only keep suggestion mode before the first whitespace.
  if (/\s/.test(contentAfterSlash)) {
    return null;
  }
  return contentAfterSlash.trim().toLowerCase();
}
function filterSuggestionsByKeyword(suggestions, keyword) {
  if (!Array.isArray(suggestions) || suggestions.length === 0) {
    return suggestions;
  }
  if (keyword === null || keyword === '') {
    return suggestions;
  }
  return suggestions.filter(function (item) {
    var valueText = typeof item.value === 'string' ? item.value.toLowerCase() : '';
    var labelText = typeof item.label === 'string' || typeof item.label === 'number' ? String(item.label).toLowerCase() : '';
    return valueText.includes(keyword) || labelText.includes(keyword);
  });
}
var ForwardSender = /*#__PURE__*/React.forwardRef(function (props, ref) {
  var _props$styles = props.styles,
    styles = _props$styles === void 0 ? {} : _props$styles,
    _props$classNames = props.classNames,
    classNames = _props$classNames === void 0 ? {} : _props$classNames,
    className = props.className,
    rootClassName = props.rootClassName,
    style = props.style,
    defaultValue = props.defaultValue,
    value = props.value,
    readOnly = props.readOnly,
    _props$enableFocusExp = props.enableFocusExpand,
    enableFocusExpand = _props$enableFocusExp === void 0 ? false : _props$enableFocusExp,
    _props$sendDisabled = props.sendDisabled,
    sendDisabled = _props$sendDisabled === void 0 ? false : _props$sendDisabled,
    _props$allowEmptySubm = props.allowEmptySubmit,
    allowEmptySubmit = _props$allowEmptySubm === void 0 ? false : _props$allowEmptySubm,
    _props$submitType = props.submitType,
    submitType = _props$submitType === void 0 ? 'enter' : _props$submitType,
    onSubmit = props.onSubmit,
    loading = props.loading,
    onCancel = props.onCancel,
    onChange = props.onChange,
    _onFocus = props.onFocus,
    _onBlur = props.onBlur,
    actions = props.actions,
    actionAffix = props.actionAffix,
    characterCountRender = props.characterCountRender,
    onKeyPress = props.onKeyPress,
    _onKeyDown = props.onKeyDown,
    suggestions = props.suggestions,
    showCharacterCount = props.showCharacterCount,
    disabled = props.disabled,
    header = props.header,
    onPaste = props.onPaste,
    allowSpeech = props.allowSpeech,
    onPasteFile = props.onPasteFile,
    onDropFile = props.onDropFile,
    components = props.components,
    rest = _objectWithoutProperties(props, _excluded);
  var _useState = useState(false),
    _useState2 = _slicedToArray(_useState, 2),
    focus = _useState2[0],
    setFocus = _useState2[1];
  var _useState3 = useState(false),
    _useState4 = _slicedToArray(_useState3, 2),
    draggingFile = _useState4[0],
    setDraggingFile = _useState4[1];
  var autoSize = React.useMemo(function () {
    return {
      maxRows: 5,
      minRows: 2
    };
  }, []);
  var _useProviderContext = useProviderContext(),
    direction = _useProviderContext.direction,
    getPrefixCls = _useProviderContext.getPrefixCls;
  var prefixCls = getPrefixCls('sender');
  var containerRef = React.useRef(null);
  var inputRef = React.useRef(null);
  var dragCounterRef = React.useRef(0);
  useProxyImperativeHandle(ref, function () {
    return {
      nativeElement: containerRef.current,
      focus: function focus() {
        var _inputRef$current;
        return (_inputRef$current = inputRef.current) === null || _inputRef$current === void 0 ? void 0 : _inputRef$current.focus();
      },
      blur: function blur() {
        var _inputRef$current2;
        return (_inputRef$current2 = inputRef.current) === null || _inputRef$current2 === void 0 ? void 0 : _inputRef$current2.blur();
      }
    };
  });
  useFocusWithin(containerRef, {
    onFocus: function onFocus() {
      setFocus(true);
      _onFocus === null || _onFocus === void 0 || _onFocus();
    },
    onBlur: function onBlur() {
      if (containerRef.current && !containerRef.current.contains(document.activeElement)) {
        setFocus(false);
        _onBlur === null || _onBlur === void 0 || _onBlur();
      }
    }
  });
  useEventListener('click', function () {
    setFocus(true);
    _onFocus === null || _onFocus === void 0 || _onFocus();
  }, {
    target: containerRef
  });
  var inputCls = "".concat(prefixCls, "-input");
  var mergedCls = classnames(prefixCls, className, rootClassName, _defineProperty(_defineProperty(_defineProperty(_defineProperty(_defineProperty({}, "".concat(prefixCls, "-rtl"), direction === 'rtl'), "".concat(prefixCls, "-disabled"), disabled), "".concat(prefixCls, "-focus"), focus && enableFocusExpand), "".concat(prefixCls, "-blur"), !focus && enableFocusExpand), "".concat(prefixCls, "-drag-over"), draggingFile));
  var actionBtnCls = "".concat(prefixCls, "-actions-btn");
  var actionListCls = "".concat(prefixCls, "-actions-list");
  var _useMergedState = useMergedState(defaultValue || '', {
      value: value
    }),
    _useMergedState2 = _slicedToArray(_useMergedState, 2),
    innerValue = _useMergedState2[0],
    setInnerValue = _useMergedState2[1];
  var triggerValueChange = function triggerValueChange(nextValue, event) {
    setInnerValue(nextValue);
    if (onChange) {
      onChange(nextValue, event);
    }
  };
  var _useSpeech = useSpeech(function (transcript) {
      triggerValueChange("".concat(innerValue, " ").concat(transcript));
    }, allowSpeech),
    _useSpeech2 = _slicedToArray(_useSpeech, 3),
    speechPermission = _useSpeech2[0],
    triggerSpeech = _useSpeech2[1],
    speechRecording = _useSpeech2[2];
  var hasSuggestions = Array.isArray(suggestions) && suggestions.length > 0;
  var slashCommandKeyword = React.useMemo(function () {
    return getSlashCommandKeyword(innerValue);
  }, [innerValue]);
  var filteredSuggestions = React.useMemo(function () {
    return filterSuggestionsByKeyword(suggestions, slashCommandKeyword);
  }, [suggestions, slashCommandKeyword]);
  var findSuggestionValueByLabel = React.useCallback(function (items, label) {
    if (!(items !== null && items !== void 0 && items.length)) {
      return undefined;
    }
    for (var _i = 0, _arr = items; _i < _arr.length; _i++) {
      var item = _arr[_i];
      if (!item || _typeof(item) !== 'object') {
        continue;
      }
      if (item.label === label && typeof item.value === 'string') {
        return item.value;
      }
      var childValue = findSuggestionValueByLabel(item.children, label);
      if (childValue) {
        return childValue;
      }
    }
    return undefined;
  }, []);
  var InputTextArea = getComponent(components, ['input'], Input.TextArea);
  var domProps = pickAttrs(rest, {
    attr: true,
    aria: true,
    data: true
  });
  var inputProps = _objectSpread(_objectSpread({}, domProps), {}, {
    ref: inputRef
  });

  // ============================ Events ============================
  var isSendDisabled = (!innerValue || !innerValue.trim()) && !allowEmptySubmit || sendDisabled;
  var triggerSend = function triggerSend() {
    if (!isSendDisabled && onSubmit && !loading) {
      onSubmit(innerValue);
    }
  };
  var triggerClear = function triggerClear() {
    triggerValueChange('');
  };

  // ============================ Submit ============================
  var isCompositionRef = React.useRef(false);
  var suggestionOpenRef = React.useRef(false);
  var onInternalCompositionStart = function onInternalCompositionStart() {
    isCompositionRef.current = true;
  };
  var onInternalCompositionEnd = function onInternalCompositionEnd(e) {
    isCompositionRef.current = false;
    if (props.maxLength) {
      var currentValue = e.target.value;
      if (currentValue.length > props.maxLength) {
        triggerValueChange(currentValue.slice(0, props.maxLength));
      }
    }
  };
  var onInternalPressEnter = function onInternalPressEnter(e) {
    var canSubmit = !isCompositionRef.current && !suggestionOpenRef.current;

    // Check for `submitType` to submit
    switch (submitType) {
      case 'enter':
        if (canSubmit && !e.shiftKey) {
          e.preventDefault();
          triggerSend();
        }
        break;
      case 'shiftEnter':
        if (canSubmit && e.shiftKey) {
          e.preventDefault();
          triggerSend();
        }
        break;
    }
  };
  var onInternalPaste = function onInternalPaste(e) {
    var _e$clipboardData;
    if (!onPasteFile) {
      onPaste === null || onPaste === void 0 || onPaste(e);
      return;
    }
    // Try to get files from clipboardData.files
    var files = Array.from(((_e$clipboardData = e.clipboardData) === null || _e$clipboardData === void 0 ? void 0 : _e$clipboardData.files) || []);
    if (files.length === 0) {
      var _e$clipboardData2;
      var items = Array.from(((_e$clipboardData2 = e.clipboardData) === null || _e$clipboardData2 === void 0 ? void 0 : _e$clipboardData2.items) || []);
      files = items.filter(function (item) {
        return item.kind === 'file';
      }).map(function (item) {
        return item.getAsFile();
      }).filter(function (file) {
        return file !== null;
      });
    }
    if (files.length > 0) {
      files.forEach(function (file) {
        return onPasteFile(file);
      });
      e.preventDefault();
    } else {
      onPaste === null || onPaste === void 0 || onPaste(e);
    }
  };
  var onInternalDragEnter = function onInternalDragEnter(e) {
    if (!onDropFile || !hasFileTransfer(e.dataTransfer)) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current += 1;
    if (!disabled && !readOnly) {
      setDraggingFile(true);
    }
  };
  var onInternalDragOver = function onInternalDragOver(e) {
    if (!onDropFile || !hasFileTransfer(e.dataTransfer)) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    e.dataTransfer.dropEffect = disabled || readOnly ? 'none' : 'copy';
  };
  var onInternalDragLeave = function onInternalDragLeave(e) {
    if (!onDropFile || !hasFileTransfer(e.dataTransfer)) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current = Math.max(0, dragCounterRef.current - 1);
    if (dragCounterRef.current === 0) {
      setDraggingFile(false);
    }
  };
  var onInternalDrop = function onInternalDrop(e) {
    if (!onDropFile || !hasFileTransfer(e.dataTransfer)) {
      return;
    }
    e.preventDefault();
    e.stopPropagation();
    dragCounterRef.current = 0;
    setDraggingFile(false);
    if (disabled || readOnly) {
      return;
    }
    var files = getFilesFromDataTransfer(e.dataTransfer);
    files.forEach(function (file) {
      return onDropFile(file);
    });
  };
  var prefix = React.useMemo(function () {
    var nodes = Array.isArray(props.prefix) ? _toConsumableArray(props.prefix) : [props.prefix];
    return nodes.filter(function (node) {
      return node !== undefined && node !== null;
    });
  }, [props.prefix]);
  var count = Math.min(innerValue.length, props.maxLength || Number.MAX_SAFE_INTEGER);
  var actionInfo = React.useMemo(function () {
    return {
      value: innerValue,
      count: count,
      maxLength: props.maxLength,
      loading: loading,
      disabled: disabled,
      sendDisabled: isSendDisabled
    };
  }, [count, disabled, innerValue, isSendDisabled, loading, props.maxLength]);
  var characterCountNode = (showCharacterCount !== null && showCharacterCount !== void 0 ? showCharacterCount : !!props.maxLength) ? /*#__PURE__*/_jsx("div", {
    className: "".concat(actionListCls, "-length"),
    children: characterCountRender ? characterCountRender(actionInfo) : props.maxLength ? "".concat(count, "/").concat(props.maxLength) : count
  }) : null;
  var actionAffixNode = typeof actionAffix === 'function' ? actionAffix(actionInfo) : actionAffix;
  var actionNode = /*#__PURE__*/_jsx(Flex, {
    className: "".concat(actionListCls, "-presets"),
    children: loading ? /*#__PURE__*/_jsx(LoadingButton, {
      loading: loading,
      disabled: !!disabled
    }) : /*#__PURE__*/_jsx(SendButton, {
      disabled: !!disabled
    })
  });
  if (typeof actions === 'function') {
    actionNode = actions(actionNode, {
      components: {
        SendButton: SendButton,
        ClearButton: ClearButton,
        LoadingButton: LoadingButton
      }
    });
  } else if (actions) {
    actionNode = actions;
  }
  var contextValue = {
    prefixCls: actionBtnCls,
    onSend: triggerSend,
    onSendDisabled: isSendDisabled,
    onClear: triggerClear,
    onClearDisabled: !innerValue,
    onCancel: onCancel,
    onCancelDisabled: !loading,
    onSpeech: function onSpeech() {
      return triggerSpeech(false);
    },
    onSpeechDisabled: !speechPermission,
    speechRecording: speechRecording,
    disabled: !!disabled
  };
  var renderInput = function renderInput(suggestionProps) {
    suggestionOpenRef.current = !!(suggestionProps !== null && suggestionProps !== void 0 && suggestionProps.open);
    return /*#__PURE__*/_jsx(InputTextArea, _objectSpread(_objectSpread({}, inputProps), {}, {
      disabled: !!disabled,
      style: styles.input,
      className: classnames(inputCls, classNames.input),
      autoSize: autoSize,
      value: innerValue.slice(0, props.maxLength || Number.MAX_SAFE_INTEGER),
      onChange: function onChange(event) {
        var nextValue = event.target.value;
        if (props.maxLength && !isCompositionRef.current && nextValue.length > props.maxLength) {
          nextValue = nextValue.slice(0, props.maxLength);
        }
        triggerValueChange(nextValue, event);
        if (hasSuggestions) {
          var nextSlashCommandKeyword = getSlashCommandKeyword(nextValue);
          var nextFilteredSuggestions = filterSuggestionsByKeyword(suggestions, nextSlashCommandKeyword);
          var nextHasFilteredSuggestions = Array.isArray(nextFilteredSuggestions) && nextFilteredSuggestions.length > 0;
          if (nextSlashCommandKeyword !== null && nextHasFilteredSuggestions) {
            var _suggestionProps$onTr;
            suggestionProps === null || suggestionProps === void 0 || (_suggestionProps$onTr = suggestionProps.onTrigger) === null || _suggestionProps$onTr === void 0 || _suggestionProps$onTr.call(suggestionProps, true);
          } else {
            var _suggestionProps$onTr2;
            suggestionProps === null || suggestionProps === void 0 || (_suggestionProps$onTr2 = suggestionProps.onTrigger) === null || _suggestionProps$onTr2 === void 0 || _suggestionProps$onTr2.call(suggestionProps, false);
          }
        }
        triggerSpeech(true);
      },
      onKeyPress: onKeyPress,
      onPressEnter: onInternalPressEnter,
      onCompositionStart: onInternalCompositionStart,
      onCompositionEnd: onInternalCompositionEnd,
      onKeyDown: function onKeyDown(event) {
        var _suggestionProps$onKe;
        if (event.key === 'Enter' && suggestionProps !== null && suggestionProps !== void 0 && suggestionProps.open && hasSuggestions) {
          var _containerRef$current, _menuItem$getAttribut;
          var root = (_containerRef$current = containerRef.current) === null || _containerRef$current === void 0 ? void 0 : _containerRef$current.ownerDocument;
          var selectedMenuItem = root === null || root === void 0 ? void 0 : root.querySelector('[role="menuitemcheckbox"][aria-checked="true"]');
          var activeMenuItem = root === null || root === void 0 ? void 0 : root.querySelector('[role="menuitem"][aria-current="true"]');
          var menuItem = selectedMenuItem instanceof HTMLElement && selectedMenuItem || activeMenuItem instanceof HTMLElement && activeMenuItem || null;
          var itemValueByPath = menuItem === null || menuItem === void 0 ? void 0 : menuItem.getAttribute('data-path-key');
          var itemLabel = menuItem === null || menuItem === void 0 || (_menuItem$getAttribut = menuItem.getAttribute('title')) === null || _menuItem$getAttribut === void 0 ? void 0 : _menuItem$getAttribut.trim();
          var itemValueByLabel = itemLabel ? findSuggestionValueByLabel(suggestions, itemLabel) : undefined;
          var selectedValue = itemValueByLabel || itemValueByPath;
          if (selectedValue) {
            var _suggestionProps$onTr3;
            event.preventDefault();
            event.stopPropagation();
            triggerValueChange("/".concat(selectedValue, " "));
            (_suggestionProps$onTr3 = suggestionProps.onTrigger) === null || _suggestionProps$onTr3 === void 0 || _suggestionProps$onTr3.call(suggestionProps, false);
            return;
          }
        }
        suggestionProps === null || suggestionProps === void 0 || (_suggestionProps$onKe = suggestionProps.onKeyDown) === null || _suggestionProps$onKe === void 0 || _suggestionProps$onKe.call(suggestionProps, event);
        _onKeyDown === null || _onKeyDown === void 0 || _onKeyDown(event);
      },
      onPaste: onInternalPaste,
      variant: "borderless",
      readOnly: readOnly
    }));
  };
  return /*#__PURE__*/_jsxs(_Fragment, {
    children: [/*#__PURE__*/_jsx(Style, {}), /*#__PURE__*/_jsxs("div", {
      ref: containerRef,
      className: mergedCls,
      style: style,
      onDragEnter: onInternalDragEnter,
      onDragOver: onInternalDragOver,
      onDragLeave: onInternalDragLeave,
      onDrop: onInternalDrop,
      children: [header && /*#__PURE__*/_jsx(SendHeaderContext.Provider, {
        value: {
          prefixCls: prefixCls,
          focus: focus,
          enableFocusExpand: enableFocusExpand
        },
        children: header
      }), /*#__PURE__*/_jsxs("div", {
        className: "".concat(prefixCls, "-content"),
        children: [hasSuggestions ? /*#__PURE__*/_jsx(Suggestion, {
          items: filteredSuggestions,
          onSelect: function onSelect(itemValue) {
            triggerValueChange("/".concat(itemValue, " "));
          },
          children: function children(suggestionProps) {
            return renderInput(suggestionProps);
          }
        }) : renderInput(), /*#__PURE__*/_jsxs("div", {
          className: "".concat(prefixCls, "-content-bottom"),
          children: [prefix.length > 0 && /*#__PURE__*/_jsx("div", {
            className: classnames("".concat(prefixCls, "-prefix"), classNames.prefix),
            style: styles.prefix,
            children: /*#__PURE__*/_jsxs(Flex, {
              gap: 8,
              children: [allowSpeech && /*#__PURE__*/_jsx(ActionButtonContext.Provider, {
                value: contextValue,
                children: /*#__PURE__*/_jsx(SpeechButton, {})
              }), prefix]
            })
          }), /*#__PURE__*/_jsxs("div", {
            className: classnames(actionListCls, classNames.actions),
            style: styles.actions,
            children: [characterCountNode, actionAffixNode, /*#__PURE__*/_jsx(ActionButtonContext.Provider, {
              value: contextValue,
              children: actionNode
            })]
          })]
        }), props.footer]
      })]
    })]
  });
});
var Sender = ForwardSender;
Sender.Header = SenderHeader;
Sender.ModeSelect = ModeSelect;
Sender.BeforeUIContainer = BeforeUIContainer;
export default Sender;