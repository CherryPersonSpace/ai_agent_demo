import { ITagRule } from '../types';
export declare const transformList: (target: Record<string, Record<string, string>>) => {
    label: string;
    desc: string;
    value: string;
}[];
export declare const DEFAULT_TAGS: ITagRule[];
