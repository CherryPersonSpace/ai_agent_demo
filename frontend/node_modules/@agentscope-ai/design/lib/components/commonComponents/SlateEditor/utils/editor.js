function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : String(i); }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }


import $i18n from "../../../../i18n";
import { message } from 'antd';
import { Editor, Element, Node, Path, Range, Text, Transforms } from 'slate';
import { ReactEditor } from 'slate-react';
/**
 * 插入节点
 * @param editor
 * @param element
 * @param options
 */
export var insertNodes = function insertNodes(editor, element, options) {
  if (!editor || !element) return;
  var selection = editor.selection;
  var point = null;
  if (selection) point = selection.focus;
  if (options !== null && options !== void 0 && options.at) point = options === null || options === void 0 ? void 0 : options.at;
  if (!point) point = {
    path: [0, 0],
    offset: 0
  };
  var _point = point,
    path = _point.path,
    offset = _point.offset;
  var parentNode = Node.get(editor, Path.parent(path));
  if (Editor.isVoid(editor, parentNode)) return;
  var text = Node.string(Node.get(editor, path));
  var preText = text.substring(0, offset - (options !== null && options !== void 0 && options.deletePrefix ? 1 : 0));
  var nextText = text.substring(offset, text.length);

  /**
   * []内是原来的内容，需要之后删掉
   * 整个过程类似于：
   * [前后] -> 后[前后] -> 插后[前后] -> 前插后[前后] -> 前插后
   */
  Editor.withoutNormalizing(editor, function () {
    editor.apply({
      type: 'insert_node',
      node: {
        text: nextText
      },
      path: path
    });
    editor.apply({
      type: 'insert_node',
      node: element,
      path: path
    });
    editor.apply({
      type: 'insert_node',
      node: {
        text: preText
      },
      path: path
    });
    var delPath = Path.next(Path.next(Path.next(path)));
    editor.apply({
      type: 'remove_node',
      path: delPath,
      node: Node.get(editor, delPath)
    });

    // 设置光标位置
    var point = {
      path: Path.next(Path.next(path)),
      offset: 0
    };
    editor.apply({
      type: 'set_selection',
      properties: null,
      newProperties: {
        anchor: point,
        focus: point
      }
    });
    ReactEditor.focus(editor);
    editor.onChange();
  });
};

/**
 * 包裹节点
 * @param editor
 * @param element
 * @param options
 * @returns
 */
export var wrapNodes = /*#__PURE__*/function () {
  var _ref = _asyncToGenerator( /*#__PURE__*/_regeneratorRuntime().mark(function _callee(editor, element, options) {
    var _options$rules;
    var defaultSelection, _ref2, tip, _ref2$selection, selection, eleTag, _options$rules2, fragment, selectedText, _copyObject, _copyObject2, start, end, textPath, textNode, startNode, endNodeSelectedText, endNode, startNodeText, startNodeSelectedText, deepIndex, outterPath, wrapTagName, outterNode, outterTag, tagName, innerPath, _options$rules3, innerNode, innerTag, _tagName;
    return _regeneratorRuntime().wrap(function _callee$(_context) {
      while (1) switch (_context.prev = _context.next) {
        case 0:
          if (!(!editor || !element)) {
            _context.next = 2;
            break;
          }
          return _context.abrupt("return");
        case 2:
          defaultSelection = editor.selection;
          _ref2 = options || {}, tip = _ref2.tip, _ref2$selection = _ref2.selection, selection = _ref2$selection === void 0 ? defaultSelection : _ref2$selection;
          eleTag = options === null || options === void 0 || (_options$rules = options.rules) === null || _options$rules === void 0 ? void 0 : _options$rules[element.type];
          if (!(selection && !Range.isCollapsed(selection))) {
            _context.next = 45;
            break;
          }
          fragment = Node.fragment(editor, selection);
          selectedText = nodeStringify(fragment);
          /**
           * 过滤未选中文本
           */
          if (!(selectedText === '')) {
            _context.next = 10;
            break;
          }
          return _context.abrupt("return");
        case 10:
          /**
           * 选区判断是否合理
           */
          _copyObject = copyObject(Range.edges(selection)), _copyObject2 = _slicedToArray(_copyObject, 2), start = _copyObject2[0], end = _copyObject2[1]; // 如果选区开始位置或结束位置在void元素上
          if (Editor.isVoid(editor, Node.get(editor, Path.parent(start.path)))) {
            start.path = Path.next(Path.parent(start.path));
            start.offset = 0;
          }
          if (Editor.isVoid(editor, Node.get(editor, Path.parent(end.path)))) {
            end.path = Path.previous(Path.parent(end.path));
            end.offset = Node.string(Node.get(editor, end.path)).length;
          }

          // 选中Element元素的情况下，Element元素前存在非空的Text节点时，选区会选不中前面的Text节点的末尾位置
          if (Path.hasPrevious(end.path) && Node.get(editor, Path.parent(start.path)) === Node.get(editor, Path.previous(end.path)) && start.offset === 0 && end.offset === 0) {
            textPath = Path.previous(Path.parent(start.path));
            textNode = Node.get(editor, textPath);
            start.path = textPath;
            start.offset = textNode.text.length;
          }

          // 处理Element在句首或者句末选区容易跨选区的情况
          if (!(Node.parent(editor, start.path) === Node.parent(editor, end.path))) {
            startNode = Node.get(editor, start.path);
            endNodeSelectedText = nodeStringify(Node.fragment(editor, {
              anchor: {
                path: end.path,
                offset: 0
              },
              focus: end
            }));
            while (Text.isText(startNode) && startNode.text === '' && selectedText === endNodeSelectedText) {
              start.path.splice(start.path.length - 1, 1, 1, 0);
              startNode = Node.get(editor, start.path);
            }
            endNode = Node.get(editor, end.path);
            startNodeText = Node.string(Node.get(editor, Range.edges(selection)[0].path));
            startNodeSelectedText = nodeStringify(Node.fragment(editor, {
              anchor: start,
              focus: {
                path: start.path,
                offset: startNodeText.length
              }
            }));
            while (Text.isText(endNode) && endNode.text === '' && selectedText === startNodeSelectedText) {
              deepIndex = end.path[end.path.length - 1];
              end.path.splice(end.path.length - 1, 1, deepIndex - 1);
              endNode = Node.get(editor, end.path);
              if (Element.isElement(endNode)) {
                end.path.push(endNode.children.length - 1);
                end.offset = startNodeText.length;
              }
            }
          }

          /**
           * 过滤选区跨节点，只要光标所在的Text节点的父节点不同，则属于跨界点
           */
          if (Node.parent(editor, start.path) === Node.parent(editor, end.path)) {
            _context.next = 18;
            break;
          }
          message.info($i18n.get({
            id: 'components.SlateEditor.utils.editor.TheSelectionRangeIsIncorrect',
            dm: '划选范围有误'
          }));
          return _context.abrupt("return");
        case 18:
          /**
           * 节点嵌套关系处理
           */
          // 检查外部标签
          outterPath = Path.parent(start.path);
          wrapTagName = (eleTag === null || eleTag === void 0 ? void 0 : eleTag.name) || getTagNameText();
          outterNode = Node.get(editor, outterPath);
          outterTag = options === null || options === void 0 || (_options$rules2 = options.rules) === null || _options$rules2 === void 0 ? void 0 : _options$rules2[outterNode.type];
        case 22:
          if (!(eleTag !== null && eleTag !== void 0 && eleTag.out && outterPath.length > 1)) {
            _context.next = 30;
            break;
          }
          if (eleTag.out.includes(outterNode.type)) {
            _context.next = 27;
            break;
          }
          // 提示文本处理
          tagName = (outterTag === null || outterTag === void 0 ? void 0 : outterTag.name) || getTagNameText();
          message.warning($i18n.get({
            id: 'components.SlateEditor.utils.editor.WraptagnameCannotBePlacedIn',
            dm: '【{wrapTagName}】不能在放在【{tagName}】内'
          }, {
            wrapTagName: wrapTagName,
            tagName: tagName
          }));
          return _context.abrupt("return");
        case 27:
          outterPath = Path.parent(outterPath);
          _context.next = 22;
          break;
        case 30:
          if (!(eleTag !== null && eleTag !== void 0 && eleTag.inner && !(Node.get(editor, start.path) === Node.get(editor, end.path)))) {
            _context.next = 42;
            break;
          }
          innerPath = Path.next(start.path);
        case 32:
          if (Path.equals(innerPath, end.path)) {
            _context.next = 42;
            break;
          }
          innerNode = Node.get(editor, innerPath);
          innerTag = options === null || options === void 0 || (_options$rules3 = options.rules) === null || _options$rules3 === void 0 ? void 0 : _options$rules3[innerNode.type];
          if (!(Element.isElement(innerNode) && !eleTag.inner.includes(innerNode.type))) {
            _context.next = 39;
            break;
          }
          // 提示文本处理
          _tagName = (innerTag === null || innerTag === void 0 ? void 0 : innerTag.name) || getTagNameText();
          message.warning($i18n.get({
            id: 'components.SlateEditor.utils.editor.WraptagnameCannotBeNestedIn',
            dm: '【{wrapTagName}】中不能嵌套【{tagName}】'
          }, {
            wrapTagName: wrapTagName,
            tagName: _tagName
          }));
          return _context.abrupt("return");
        case 39:
          innerPath = Path.next(innerPath);
          _context.next = 32;
          break;
        case 42:
          /**
           * 嵌套节点
           */
          Editor.withoutNormalizing(editor, function () {
            // 1、获取选中部分内容
            var parentNode = Node.parent(editor, start.path);
            var selectedNodes = Node.fragment(parentNode, {
              anchor: {
                path: [start.path[start.path.length - 1]],
                offset: start.offset
              },
              focus: {
                path: [end.path[end.path.length - 1]],
                offset: end.offset
              }
            });
            // 2、删除选中部分内容
            var startText = Node.string(Node.get(editor, start.path));
            if (Path.equals(start.path, end.path)) {
              editor.apply({
                type: 'remove_text',
                path: start.path,
                offset: start.offset,
                text: startText.substring(start.offset, end.offset)
              });
            } else {
              editor.apply({
                type: 'remove_text',
                path: start.path,
                offset: start.offset,
                text: startText.substring(start.offset, startText.length)
              });
              var deletePath = Path.next(start.path);
              while (!Path.equals(deletePath, end.path)) {
                editor.apply({
                  type: 'remove_node',
                  path: deletePath,
                  node: Node.get(editor, deletePath)
                });
                end.path = Path.previous(end.path);
              }
              var endText = Node.string(Node.get(editor, end.path));
              editor.apply({
                type: 'remove_text',
                path: end.path,
                offset: 0,
                text: endText.substring(0, end.offset)
              });
            }

            // 如果选区不在同一Text节点，需要将相邻的Text节点合并
            if (!Path.equals(start.path, end.path)) {
              var _endNode = Node.get(editor, end.path);
              editor.apply({
                type: 'remove_node',
                path: end.path,
                node: _endNode
              });
              var _endText = Node.string(_endNode);
              editor.apply({
                type: 'insert_text',
                path: start.path,
                offset: start.offset,
                text: _endText
              });
            }

            // 3、将选中部分包裹插入
            insertNodes(editor, _objectSpread(_objectSpread({}, element), {}, {
              children: selectedNodes
            }), {
              at: start,
              rules: options === null || options === void 0 ? void 0 : options.rules
            });
          });
          _context.next = 46;
          break;
        case 45:
          if (tip) message.warning(tip);
        case 46:
        case "end":
          return _context.stop();
      }
    }, _callee);
  }));
  return function wrapNodes(_x, _x2, _x3) {
    return _ref.apply(this, arguments);
  };
}();

/**
 * 拆开节点
 * @param editor
 * @param nodes
 */
export var unwrapNodes = function unwrapNodes(editor, path, cb) {
  if (!editor || !path) return;
  var node = Node.get(editor, path);
  var children = copyObject(node.children).reverse();
  Editor.withoutNormalizing(editor, function () {
    // 删除节点
    editor.apply({
      type: 'remove_node',
      node: node,
      path: path
    });
    // 将子节点插入原先路径
    children.forEach(function (child) {
      editor.apply({
        type: 'insert_node',
        node: child,
        path: path
      });
    });
    // 将Text节点进行合并
    var parentPath = Path.parent(path);
    var parentNode = Node.get(editor, parentPath);
    // 从前往后合并Text节点
    var i = 0;
    while (Node.has(parentNode, [i + 1])) {
      if (Text.isText(Node.get(parentNode, [i])) && Text.isText(Node.get(parentNode, [i + 1]))) {
        var childPath = _toConsumableArray(parentPath);
        childPath.push(i + 1);

        // 将当前Text节点跟前一个Text节点合并，先删除Text节点再向前一个Text节点末尾插入文本
        var mergeNode = Node.get(editor, childPath);
        var mergeText = Node.string(mergeNode);
        editor.apply({
          type: 'remove_node',
          path: childPath,
          node: mergeNode
        });
        var targetPath = Path.previous(childPath);
        var targetText = Node.string(Node.get(editor, targetPath));
        editor.apply({
          type: 'insert_text',
          path: Path.previous(childPath),
          offset: targetText.length,
          text: mergeText
        });

        // 合并节点之后，父节点的子节点，需要tagList
        parentNode = Node.get(editor, parentPath);
      } else {
        i++;
      }
    }
    cb === null || cb === void 0 || cb();
    Path.previous(path);
  });
};

/**
 * 节点设置新的属性
 * @param editor
 * @param nodeProps 新节点属性
 * @param options 设置的节点路径
 */
export var setNodes = function setNodes(editor, nodeProps, options) {
  var at = options.at;
  editor.apply({
    type: 'set_node',
    path: at,
    properties: nodeProps,
    newProperties: nodeProps
  });
  // 重新渲染
  editor.onChange();
};

/**
 * 替换节点
 * @param editor
 * @param node 新节点
 * @param path 久节点路径
 */
export var replaceNode = function replaceNode(editor, node, path) {
  editor.apply({
    type: 'remove_node',
    path: path,
    node: Node.get(editor, path)
  });
  editor.apply({
    type: 'insert_node',
    path: path,
    node: node
  });
};

/**
 * 重新设置全文内容，content不传则清空全文
 * @param editor
 * @param content
 */
export var setEditorContent = function setEditorContent(editor, content) {
  if (!editor) return;
  Editor.withoutNormalizing(editor, function () {
    var children = copyObject(editor.children);
    children.forEach(function (child) {
      editor.apply({
        type: 'remove_node',
        path: [0],
        node: child
      });
    });
    var newChildren = content ? copyObject(content) : [{
      type: 's',
      children: [{
        text: ''
      }]
    }];
    newChildren.reverse();
    newChildren.forEach(function (newChild) {
      editor.apply({
        type: 'insert_node',
        path: [0],
        node: newChild
      });
    });
  });
  // 重新渲染
  editor.onChange();
};

/**
 * 获取节点字符串内容
 * @param {Node[]} nodes
 * @returns {Node[]}
 */
export var nodeStringify = function nodeStringify() {
  var nodes = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  return nodes.reduce(function (result, node) {
    return result + Node.string(node);
  }, '');
};

/**
 * 深克隆对象（不涉及克隆undefined、function、symbol等类型属性）
 * @param {object | array | string | number} obj
 * @returns {object | array | string | number}
 */
export var copyObject = function copyObject(object) {
  return JSON.parse(JSON.stringify(object));
};

/**
 * 获取标签对应名称文本
 * @param {CustomElement} element
 * @return {string}
 */
export var getTagNameText = function getTagNameText() {
  var wrapTagName = '';
  return wrapTagName;
};
export var mapRender = function mapRender(list) {
  var _list$;
  return (_list$ = list[0]) === null || _list$ === void 0 || (_list$ = _list$.children) === null || _list$ === void 0 ? void 0 : _list$.map(function (item) {
    return item.children.map(function (item) {
      return item.text;
    }).join('');
  }).join('');
};
export var renderTime = function renderTime() {
  var time = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 0;
  // time是秒级别
  var seconds = time % 60;
  var minutes = parseInt("".concat(time / 60));
  var hour = 0;
  if (minutes > 60) {
    minutes = minutes % 60;
    hour = parseInt("".concat(minutes / 60));
  }
  return "".concat(hour ? "".concat(covertTime(hour), ":") : '').concat(covertTime(minutes), ":").concat(covertTime(seconds));
};
export var covertTime = function covertTime(time) {
  if (time >= 10) return "".concat(time);
  return "0".concat(time);
};
export var findNodePath = function findNodePath(editor, item) {
  if (!editor || !item) return undefined;
  return ReactEditor.findPath(editor, item);
};
export var setCursorRemove = function setCursorRemove(editor) {
  if (!editor) return;
  Editor.withoutNormalizing(editor, function () {
    var endPath = Editor.end(editor, []);
    Transforms.select(editor, endPath);
    ReactEditor.focus(editor);
    editor.onChange();
  });
};