function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }
function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }
function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }
function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }
function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }
function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) arr2[i] = arr[i]; return arr2; }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(obj, key, value) { key = _toPropertyKey(key); if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : String(i); }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
import { Element } from 'slate';
import { DEFAULT_TAGS } from "./constant";

/**
+ * xml特殊字符转义
+ * @param xml 
+ */
export var xmlEscape = function xmlEscape(xml) {
  return xml.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');
};
export var filterEscape = function filterEscape(xml) {
  return xml.replace(/&/g, '&amp;').replace(/{/g, '').replace(/}/g, '').replace(/"/g, '&quot;').replace(/'/g, '&apos;');
};

/**
 * xml转义字符还原
 * @param xml
 */
export var xmlReverseEscape = function xmlReverseEscape(xml) {
  return xml.replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>').replace(/&quot;/g, '"').replace(/&apos;/g, "'");
};

/**
 * 序列化
 * @param value
 * @returns
 */
export var serialize = function serialize(value, options) {
  var result = '';
  var _ref = options || {},
    _ref$tagRules = _ref.tagRules,
    tagRules = _ref$tagRules === void 0 ? {} : _ref$tagRules;
  value.forEach(function (node, index) {
    if (Element.isElement(node)) {
      var targetTagRule = tagRules[node.type];
      if (!targetTagRule) {
        result += xmlEscape(node.text);
      } else {
        switch (targetTagRule.code) {
          case 'image':
            result += "![".concat(node.fileName, "](").concat(node.src, ")");
            break;
          case 'pdf':
            result += "[".concat(node.fileName, "](").concat(node.src, ")");
            break;
          case 's':
            result += "".concat(serialize(node.children, options)).concat(index < value.length - 1 ? '\n' : '');
            break;
          case 'var':
            result += "${".concat(node.code, "}");
          default:
            break;
        }
      }
    } else {
      result += xmlEscape(node.text);
    }
  });
  return result;
};

/**
 * 获取xml格式字符串
 * @returns
 */
export var getEditorText = function getEditorText(editor, options) {
  if (editor && !!editor.children.length) {
    var text = serialize(editor.children, options);
    if (text === '<s></s>') return '';
    return xmlReverseEscape(text);
  }
  return '';
};
export var transformVarText = function transformVarText(str) {
  if (!str) return '';
  var resultStr = '';
  var tempStr = xmlEscape(str).replace(/\$\{([^{}]+?)\}/g, function (_, varName) {
    // 替换成指定的内容
    return "<var code=\"".concat(varName, "\" label=\"${").concat(varName, "}\" />");
  });
  tempStr.split('\n').forEach(function (item) {
    resultStr += "<s>".concat(item, "</s>");
  });
  return "<speak>".concat(resultStr, "</speak>");
};

/**
 	+ * xml的Dom对象转换成json对象
 	+ * @param xmlObj 
 	+ */
export var xmlObj2json = function xmlObj2json(xmlObj) {
  var _xmlObj$childNodes;
  var jsonObj = [];
  if ((xmlObj === null || xmlObj === void 0 || (_xmlObj$childNodes = xmlObj.childNodes) === null || _xmlObj$childNodes === void 0 ? void 0 : _xmlObj$childNodes.length) > 0) {
    xmlObj.childNodes.forEach(function (node, index, array) {
      var nodeType = node.nodeType,
        nodeName = node.nodeName;
      if (nodeType === 1) {
        var attributes = {};
        for (var i = 0; i < node.attributes.length; i++) {
          var _node$attributes$i = node.attributes[i],
            attributeName = _node$attributes$i.nodeName,
            attributeValue = _node$attributes$i.nodeValue;
          attributes[attributeName] = attributeValue;
        }
        var nodeObject = _objectSpread({
          type: nodeName
        }, attributes);
        if (node.childNodes.length > 0) {
          // 非闭合标签节点
          nodeObject.children = xmlObj2json(node);
        } else {
          // 闭合节点
          nodeObject.children = [{
            text: ''
          }];
        }
        jsonObj.push(nodeObject);

        // 规范化
        if (nodeName !== 's') {
          if (index > 0 && array[index - 1].nodeType === 1) {
            jsonObj.unshift({
              text: ''
            });
          }
          if (index === array.length - 1) {
            jsonObj.push({
              text: ''
            });
          }
        }
      } else if (nodeType === 3) {
        // 叶子结点
        jsonObj.push({
          text: node.nodeValue || ''
        });
      }
    });
  }
  return jsonObj;
};

/**
 * 反序列化
 * @param text
 */
export var text2json = function text2json(text) {
  if (!text) return undefined;
  var xmlObj = new DOMParser().parseFromString(text, 'text/xml');
  return xmlObj2json(xmlObj);
};
export var wrapSpeakTag = function wrapSpeakTag(val) {
  if (!val || val.includes('</speak>')) return val;
  var tempStr = val;
  if (!tempStr.includes('<s>')) tempStr = "<s>".concat(tempStr, "</s>");
  return "<speak>".concat(tempStr, "</speak>");
};
export var transformTagRules = function transformTagRules() {
  var rules = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : [];
  var newTagMap = {};
  [].concat(_toConsumableArray(rules), _toConsumableArray(DEFAULT_TAGS)).forEach(function (item) {
    if (newTagMap[item.code]) return;
    newTagMap[item.code] = item;
  });
  return newTagMap;
};