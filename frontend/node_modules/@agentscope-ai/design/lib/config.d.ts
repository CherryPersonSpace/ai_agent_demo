import { ConfigProviderProps } from 'antd';
import { Locale } from 'antd/es/locale';
export declare const DEFAULT_ANT_PREFIX = "ant";
export declare const DEFAULT_BL_PREFIX = "bl";
export declare const DEFAULT_SPARK_PREFIX = "spark";
export interface CommonConfig {
    /** 用户从BLConfigProvider输入的前缀 */
    prefix?: string;
    /** antd组件前缀修改 */
    antPrefix?: string;
    /** legacy: bailian-ui组件前缀修改 */
    blPrefix?: string;
    /** spark组件前缀修改 */
    sparkPrefix?: string;
    /** 当Spark组件和antd组件重名时，是否使用Spark组件而不是antd的组件 */
    configProviderProps: ConfigProviderProps;
    locale?: Locale;
    /** iconfont 地址 */
    iconfont?: string;
    isDarkMode?: boolean;
    /** 主题配置，供 staticRenderer 等静态方法使用 */
    theme?: ConfigProviderProps['theme'];
}
export declare function setCommonConfig(newConf: CommonConfig): void;
export declare function getCommonConfig(): CommonConfig;
export declare function useCommonConfig(): CommonConfig;
