interface useGlobalStyleParams {
    /** 要插入的css样式 */
    globalStyle: string;
    /** 标签上增加name标记 */
    name?: string;
}
export default function useGlobalStyle({ globalStyle, name, }: useGlobalStyleParams): void;
export {};
