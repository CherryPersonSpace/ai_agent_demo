export declare function getDataProp(data: any, prop: string | string[]): any;
/**
 * JSON字符串转对象
 * @param {string} str 被转换的string对象
 * @param {obj} [def={}] 当转换失败或者str为空时，传递给上层的对象
 * @returns {object}
 */
export declare function JsonParse(str: string, def?: {}): any;
/**
 * 清理数据中的指定key或者以_fe_开头的字段
 * @param {object} data 被清理的数据
 * @param {string[]} [keys] 要清理掉的key列表,默认为`['class']`
 * @returns {object} 返回清理后的数据
 */
export declare function clearData(data: any, keys?: string[]): any;
