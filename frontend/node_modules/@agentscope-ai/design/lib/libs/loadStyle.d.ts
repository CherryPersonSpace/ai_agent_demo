/**
 * @function loadStyle(url, options) 加载style方法
 * @param url 样式文件地址
 * @param options 参数
 * @param options.head 是否插入head标签中，默认false则插入body标签中
 */
export declare function loadStyle(url: string | string[], opts: any): any;
