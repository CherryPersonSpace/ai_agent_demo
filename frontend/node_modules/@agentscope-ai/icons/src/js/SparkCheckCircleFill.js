import React from 'react';
import '../index.css';
export default function SparkCheckCircleFill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-checkCircle-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-checkCircle-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-checkCircle-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M128 512C128 299.9232 299.9232 128 512 128s384 171.9232 384 384-171.9232 384-384 384S128 724.0768 128 512z m537.6-134.4a32 32 0 0 0-22.6272 9.3728L460.8 569.1456l-79.7728-79.7728A32.0064 32.0064 0 0 0 326.4 512a32 32 0 0 0 9.3728 22.6272l102.4 102.4c12.496 12.496 32.7584 12.496 45.2544 0l204.8-204.8A32.0064 32.0064 0 0 0 665.6 377.6z"></path>' },
      }))
    ],
  }))
}