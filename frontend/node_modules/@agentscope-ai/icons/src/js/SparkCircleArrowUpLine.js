import React from 'react';
import '../index.css';
export default function SparkCircleArrowUpLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-circleArrowUp-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-circleArrowUp-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-circleArrowUp-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M128 512a384 384 0 1 1 768 0 384 384 0 0 1-768 0z m64 0a320 320 0 1 0 640 0 320 320 0 0 0-640 0z"></path><path d="M512 471.9104l105.3696 105.3696a32 32 0 1 0 45.2608-45.2608l-128-128a32 32 0 0 0-45.2608 0l-128 128a32 32 0 0 0 45.2608 45.2096L512 471.9616z"></path>' },
      }))
    ],
  }))
}