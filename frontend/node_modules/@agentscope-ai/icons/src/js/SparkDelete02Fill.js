import React from 'react';
import '../index.css';
export default function SparkDelete02Fill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-delete02-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-delete02-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-delete02-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M128 512C128 299.9232 299.9232 128 512 128s384 171.9232 384 384-171.9232 384-384 384S128 724.0768 128 512z m551.7632 0.0032a31.984 31.984 0 0 0-9.3728-22.6272 31.984 31.984 0 0 0-22.6272-9.3728h-271.5264a31.9904 31.9904 0 0 0-22.6304 9.3728 32.0064 32.0064 0 1 0 22.6272 54.6272h271.5296a32 32 0 0 0 32-32z"></path>' },
      }))
    ],
  }))
}