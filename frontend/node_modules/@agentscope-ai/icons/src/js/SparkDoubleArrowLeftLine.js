import React from 'react';
import '../index.css';
export default function SparkDoubleArrowLeftLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-doubleArrowLeft-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-doubleArrowLeft-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-doubleArrowLeft-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M489.3696 233.3696a32 32 0 1 1 45.2608 45.2608L301.2608 512l233.3696 233.3696a32 32 0 1 1-45.2608 45.2608l-256-256a32 32 0 0 1 0-45.2608l256-256z m256 0a32 32 0 1 1 45.2608 45.2608L557.2608 512l233.3696 233.3696a31.9488 31.9488 0 1 1-45.2608 45.2608l-256-256a32 32 0 0 1 0-45.2608l256-256z"></path>' },
      }))
    ],
  }))
}