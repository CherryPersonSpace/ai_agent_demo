import React from 'react';
import '../index.css';
export default function SparkDoubleleftLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-doubleleft-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-doubleleft-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-doubleleft-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M314.0544 512l233.3728-233.3728a32 32 0 1 0-45.2544-45.2544l-256 256c-12.496 12.496-12.496 32.7584 0 45.2544l256 256a32.0064 32.0064 0 0 0 45.2544 0 32 32 0 0 0 0-45.2544L314.0544 512z m230.4 0l233.376-233.3728A32.0064 32.0064 0 0 0 755.2 224a32 32 0 0 0-22.624 9.3728l-256.0032 256c-12.496 12.496-12.496 32.7584 0 45.2544l256 256a32.0032 32.0032 0 0 0 45.2544-45.2544L544.4544 512z"></path>' },
      }))
    ],
  }))
}