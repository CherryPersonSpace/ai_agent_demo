import React from 'react';
import '../index.css';
export default function SparkDownAdditionLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-downAddition-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-downAddition-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-downAddition-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M224 134.4h576c35.328 0 64 28.672 64 64V409.6c0 35.328-28.672 64-64 64H224c-35.328 0-64-28.672-64-64V198.4c0-35.328 28.672-64 64-64z m0 64V409.6h576V198.4H224z"></path><path d="M864 550.4m0 64l0 211.2q0 64-64 64l-576 0q-64 0-64-64l0-211.2q0-64 64-64l576 0q64 0 64 64Z"></path>' },
      }))
    ],
  }))
}