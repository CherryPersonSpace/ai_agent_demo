import React from 'react';
import '../index.css';
export default function SparkDownFill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-down-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-down-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-down-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M128 512C128 299.9232 299.9232 128 512 128s384 171.9232 384 384-171.9232 384-384 384S128 724.0768 128 512z m384 40.08l-105.3664-105.3664A32.0064 32.0064 0 0 0 352 469.3344c0 8.4864 3.3728 16.624 9.3792 22.6336l127.9936 127.9936c12.496 12.496 32.7584 12.496 45.2544 0l128.0192-128.0192 0.272-0.2752a32 32 0 0 0-45.5456-44.96L512 552.08z"></path>' },
      }))
    ],
  }))
}