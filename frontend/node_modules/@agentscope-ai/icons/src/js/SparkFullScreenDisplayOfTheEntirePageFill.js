import React from 'react';
import '../index.css';
export default function SparkFullScreenDisplayOfTheEntirePageFill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-fullScreenDisplayOfTheEntirePage-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-fullScreenDisplayOfTheEntirePage-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-fullScreenDisplayOfTheEntirePage-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M227.2 742.4c0 21.2064 17.1936 38.4 38.4 38.4h166.4V240H265.6c-21.2064 0-38.4 17.1936-38.4 38.4v464z m572.8 0c0 21.2064-17.1936 38.4-38.4 38.4h-284.8V240h284.8c21.2064 0 38.4 17.1936 38.4 38.4v464z"></path>' },
      }))
    ],
  }))
}