import React from 'react';
import '../index.css';
export default function SparkLeftArrowLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-leftArrow-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-leftArrow-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-leftArrow-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M211.2032 512c0-17.6736 14.3264-32 32-32H793.6c17.6736 0 32 14.3264 32 32s-14.3264 32-32 32H243.2032c-17.6736 0-32-14.3264-32-32z"></path><path d="M521.8272 278.6272L288.4544 512l233.376 233.3728a32 32 0 0 1-45.2576 45.2544l-256-256c-12.496-12.496-12.496-32.7584 0-45.2544l256-256A32.0064 32.0064 0 0 1 531.2 256a32 32 0 0 1-9.3728 22.6272z"></path>' },
      }))
    ],
  }))
}