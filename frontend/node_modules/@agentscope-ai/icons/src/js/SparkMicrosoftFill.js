import React from 'react';
import '../index.css';
export default function SparkMicrosoftFill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-microsoft-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-microsoft-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-microsoft-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M153.6 493.098667V294.997333l235.008-49.834666v244.394666l-235.008 3.584zM819.2 153.6v330.069333l-391.594667 5.717334V236.970667L819.2 153.6zM153.6 530.901333l235.008 3.413334v256.853333L153.6 747.605333v-216.746666z m665.6 9.386667V870.4l-391.594667-71.978667v-263.637333l391.594667 5.546667z"></path>' },
      }))
    ],
  }))
}