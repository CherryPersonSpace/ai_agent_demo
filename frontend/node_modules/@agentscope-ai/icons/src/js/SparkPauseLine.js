import React from 'react';
import '../index.css';
export default function SparkPauseLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-pause-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-pause-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-pause-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M326.4 201.6c17.6736 0 32 14.3264 32 32v556.8c0 17.6736-14.3264 32-32 32s-32-14.3264-32-32V233.6c0-17.6736 14.3264-32 32-32zM697.6 201.6c17.6736 0 32 14.3264 32 32v556.8c0 17.6736-14.3264 32-32 32s-32-14.3264-32-32V233.6c0-17.6736 14.3264-32 32-32z"></path>' },
      }))
    ],
  }))
}