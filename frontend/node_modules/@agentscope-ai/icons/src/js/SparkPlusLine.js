import React from 'react';
import '../index.css';
export default function SparkPlusLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-plus-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-plus-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-plus-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M480 774.3968L480.032 249.6a32 32 0 0 1 64 0L544 774.4a32.0096 32.0096 0 0 1-32 32 32 32 0 0 1-32-32.0032z"></path><path d="M217.6 512c0-17.6736 14.3264-32 32-32h524.8c17.6736 0 32 14.3264 32 32s-14.3264 32-32 32H249.6c-17.6736 0-32-14.3264-32-32z"></path>' },
      }))
    ],
  }))
}