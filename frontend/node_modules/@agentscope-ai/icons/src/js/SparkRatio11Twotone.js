import React from 'react';
import '../index.css';
export default function SparkRatio11Twotone(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-ratio11-twotone';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-ratio11-twotone',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-ratio11-twotone',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M243.2 742.4V281.6c0-21.2064 17.1936-38.4 38.4-38.4h460.8c21.2064 0 38.4 17.1936 38.4 38.4v460.8c0 21.2064-17.1936 38.4-38.4 38.4H281.6c-21.2064 0-38.4-17.1936-38.4-38.4z"></path>' },
      }))
    ],
  }))
}