import React from 'react';
import '../index.css';
export default function SparkRightAdditionLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-rightAddition-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-rightAddition-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-rightAddition-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M134.4 800V224c0-35.328 28.672-64 64-64H409.6c35.328 0 64 28.672 64 64v576c0 35.328-28.672 64-64 64H198.4c-35.328 0-64-28.672-64-64z m64 0H409.6V224H198.4v576z"></path><path d="M550.4 800V224c0-35.328 28.672-64 64-64h211.2c35.328 0 64 28.672 64 64v576c0 35.328-28.672 64-64 64H614.4c-35.328 0-64-28.672-64-64z"></path>' },
      }))
    ],
  }))
}