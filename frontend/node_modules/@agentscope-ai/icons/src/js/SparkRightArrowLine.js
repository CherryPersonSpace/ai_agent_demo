import React from 'react';
import '../index.css';
export default function SparkRightArrowLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-rightArrow-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-rightArrow-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-rightArrow-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M716.3456 544l-201.3728 201.376a32 32 0 1 0 45.2544 45.2512l256-256a31.8816 31.8816 0 0 0 9.0496-18.08 32.224 32.224 0 0 0-1.0752-13.92 31.8496 31.8496 0 0 0-7.9744-13.2544l-256-256a32 32 0 1 0-45.2544 45.2544l201.3728 201.376H243.2c-17.6736 0-32 14.3232-32 32 0 17.6704 14.3264 32 32 32h473.1456z"></path>' },
      }))
    ],
  }))
}