import React from 'react';
import '../index.css';
export default function SparkSortLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-sort-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-sort-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-sort-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M849.92 619.6736a32 32 0 0 1-9.3696 22.6304l-215.4496 215.4496a32 32 0 0 1-54.5792-24.1664V185.6a32 32 0 1 1 64 0v572.2624l160.768-160.768a32 32 0 0 1 54.6304 22.5792zM422.4 867.328a32 32 0 0 1-32-32V266.1376l-160.768 160.768a32 32 0 1 1-45.2608-45.2096l215.3984-215.4496a32 32 0 0 1 54.6304 22.6304v646.4c0 17.664-14.336 32-32 32z"></path>' },
      }))
    ],
  }))
}