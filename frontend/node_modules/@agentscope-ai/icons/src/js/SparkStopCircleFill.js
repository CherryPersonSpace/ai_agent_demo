import React from 'react';
import '../index.css';
export default function SparkStopCircleFill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-stopCircle-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-stopCircle-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-stopCircle-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M156.8 512C156.8 315.8272 315.8272 156.8 512 156.8s355.2 159.0272 355.2 355.2-159.0272 355.2-355.2 355.2S156.8 708.1728 156.8 512z m134.4-188.8a32 32 0 0 0 9.3728 22.6272l377.6 377.6a32.0032 32.0032 0 0 0 45.2544-45.2544l-377.6-377.6A32 32 0 0 0 323.2 291.2c-17.6736 0-32 14.3264-32 32z"></path>' },
      }))
    ],
  }))
}