import React from 'react';
import '../index.css';
export default function SparkTextLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-text-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-text-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-text-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M544.0096 240v563.2H656c17.6736 0 32 14.3264 32 32s-14.3264 32-32 32h-288c-17.6736 0-32-14.3264-32-32s14.3264-32 32-32h112.0096V240H256v57.6c0 17.6736-14.3264 32-32 32s-32-14.3264-32-32V208c0-17.6736 14.3264-32 32-32h576c17.6736 0 32 14.3264 32 32v89.6c0 17.6736-14.3264 32-32 32s-32-14.3264-32-32V240h-223.9904z"></path>' },
      }))
    ],
  }))
}