import React from 'react';
import '../index.css';
export default function SparkUpArrowLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-upArrow-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-upArrow-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-upArrow-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M518.4 204.8032c17.6736 0 32 14.3264 32 32v550.4c0 17.6704-14.3264 32-32 32s-32-14.3296-32-32V236.8c0-17.6736 14.3264-32 32-32z"></path><path d="M751.7728 515.4304L518.4 282.0544l-233.3728 233.376a32 32 0 0 1-45.2544-45.2576l256-256c12.496-12.496 32.7584-12.496 45.2544 0l256 256A32.0064 32.0064 0 0 1 774.4 524.8a32 32 0 0 1-22.6272-9.3696z"></path>' },
      }))
    ],
  }))
}