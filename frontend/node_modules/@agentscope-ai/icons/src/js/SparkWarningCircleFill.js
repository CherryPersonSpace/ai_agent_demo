import React from 'react';
import '../index.css';
export default function SparkWarningCircleFill(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-warningCircle-fill';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-warningCircle-fill',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-warningCircle-fill',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M896 512c0 212.0768-171.9232 384-384 384S128 724.0768 128 512 299.9232 128 512 128s384 171.9232 384 384z m-414.6432 35.2576a30.7072 30.7072 0 0 0 61.2864 0l11.2896-176.1504C555.4848 346.8928 536.2656 326.4 512 326.4c-24.2656 0-43.4848 20.4928-41.9328 44.7072l11.2896 176.1504zM512 614.4c-24.7424 0-44.8 20.0576-44.8 44.8s20.0576 44.8 44.8 44.8 44.8-20.0576 44.8-44.8-20.0576-44.8-44.8-44.8z"></path>' },
      }))
    ],
  }))
}