import React from 'react';
import '../index.css';
export default function SparkYoujiantouRightArrowLine(props) {
  const { size, spin, ...restProps } = props;
  const icon = 'spark-icon spark-icon-spark-youjiantou-rightArrow-line';
  const className = [
    icon,
    spin ? 'spark-icon-spin' : '',
    props.className || ''
  ].filter(Boolean).join(' ');

  let mergedFontSize = undefined;
  if(typeof props.size === 'number') {
    mergedFontSize = props.size;
  } else {
    mergedFontSize = props.style?.fontSize;
  }
  
  return React.createElement('span', Object.assign({}, restProps, {
    className: className,
    style: {
      ...props.style,
      fontSize: mergedFontSize,
      cursor: props.onClick ? 'pointer' : props.style?.cursor
    },
    role: 'img',
    'aria-label': 'spark-youjiantou-rightArrow-line',
    'data-spark-icon': true, // 标识为 spark-icon 组件
    children: [
      React.createElement('svg', Object.assign({
        key: 'spark-youjiantou-rightArrow-line',
        width: '1em',
        height: '1em',
        viewBox: '0 0 1024 1024',
        overflow: 'hidden',
        fill: 'currentColor',
        'aria-hidden': 'true',
        dangerouslySetInnerHTML: { __html: '<path d="M243.2 544a32 32 0 1 1 0-64h473.1392l-201.3696-201.3696a32 32 0 1 1 45.2608-45.2608l256 256a31.8208 31.8208 0 0 1 8.0384 31.744 31.8464 31.8464 0 0 1-8.0384 13.5168l-256 256a32 32 0 0 1-45.2608-45.2608l201.3696-201.3696H243.2z"></path>' },
      }))
    ],
  }))
}