"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
exports.get = getCookie;
exports.getJSON = getJSON;
exports.remove = removeCookie;
exports.set = setCookie;
var SameSite;

(function (SameSite) {
  SameSite["strict"] = "strict";
  SameSite["lax"] = "lax";
})(SameSite || (SameSite = {}));

function setCookie(key, value) {
  var _ref = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {},
      _ref$path = _ref.path,
      path = _ref$path === void 0 ? '/' : _ref$path,
      domain = _ref.domain,
      expires = _ref.expires,
      _ref$maxAge = _ref.maxAge,
      maxAge = _ref$maxAge === void 0 ? 60 * 60 * 14 * 30 : _ref$maxAge,
      secure = _ref.secure,
      sameSite = _ref.sameSite;

  if (!key) return;
  value = encodeURIComponent(String(value)).replace(/%(23|24|26|2B|3A|3C|3E|3D|2F|3F|40|5B|5D|5E|60|7B|7D|7C)/g, decodeURIComponent);
  key = encodeURIComponent(String(key));
  key = key.replace(/%(23|24|26|2B|5E|60|7C)/g, decodeURIComponent);
  key = key.replace(/[()]/g, escape);
  var attrs = [];
  if (path) attrs.push("path=".concat(path)); // path 非常重要, 默认是  location.pathname , 在删除或者修改 cookie 时候, path 没有对齐是无法修改 cookie 的.

  if (domain) attrs.push("domain=".concat(domain));
  if (expires) attrs.push("expires=".concat(expires)); //! important:"max-age" is not supported by IE6/7/8

  if (maxAge) attrs.push("max-age=".concat(maxAge));
  if (secure) attrs.push('secure');
  if (sameSite) attrs.push("samesite=".concat(sameSite));
  document.cookie = "".concat(key, "=").concat(value, ";").concat(attrs.join(';'));
}

function getCookie(key) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  var COOKIE_SPLITTER = '; ';
  var KEY_VALUE_SPLITTER = '=';
  var result = key ? undefined : {};
  var cookies = document.cookie ? document.cookie.split(COOKIE_SPLITTER) : [];
  var REDCode = /(%[0-9A-Z]{2})+/g;

  for (var i = 0; i < cookies.length; i++) {
    var parts = cookies[i].split(KEY_VALUE_SPLITTER);
    var cookie = parts.slice(1).join(KEY_VALUE_SPLITTER);

    if (!options.json && cookie.charAt(0) === '"') {
      cookie = cookie.slice(1, -1);
    }

    try {
      var name = parts[0].replace(REDCode, decodeURIComponent);
      cookie = cookie.replace(REDCode, decodeURIComponent);

      if (options.json) {
        try {
          cookie = JSON.parse(cookie);
        } catch (e) {} // eslint-disable-line

      }

      if (key === name) {
        result = cookie;
        break;
      }

      if (!key) {
        result[name] = cookie;
      }
    } catch (e) {} // eslint-disable-line

  }

  return result;
}

function removeCookie(key) {
  var attrs = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
  setCookie(key, '', Object.assign(attrs, {
    expires: 'Thu, 01 Jan 1970 00:00:00 GMT',
    maxAge: -1
  }));
}

function getJSON() {
  return getCookie(undefined, {
    json: true
  });
}

var Cookie = {
  set: setCookie,
  get: getCookie,
  getJSON: getJSON,
  remove: removeCookie
};
var _default = Cookie;
exports.default = _default;
//# sourceMappingURL=cookie.js.map