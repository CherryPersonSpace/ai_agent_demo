"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = exports.LOCALE_KEY_NAME = void 0;

var Cookie = _interopRequireWildcard(require("./cookie"));

var Util = _interopRequireWildcard(require("./util"));

function _getRequireWildcardCache(nodeInterop) { if (typeof WeakMap !== "function") return null; var cacheBabelInterop = new WeakMap(); var cacheNodeInterop = new WeakMap(); return (_getRequireWildcardCache = function _getRequireWildcardCache(nodeInterop) { return nodeInterop ? cacheNodeInterop : cacheBabelInterop; })(nodeInterop); }

function _interopRequireWildcard(obj, nodeInterop) { if (!nodeInterop && obj && obj.__esModule) { return obj; } if (obj === null || _typeof(obj) !== "object" && typeof obj !== "function") { return { default: obj }; } var cache = _getRequireWildcardCache(nodeInterop); if (cache && cache.has(obj)) { return cache.get(obj); } var newObj = {}; var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor; for (var key in obj) { if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) { var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null; if (desc && (desc.get || desc.set)) { Object.defineProperty(newObj, key, desc); } else { newObj[key] = obj[key]; } } } newObj.default = obj; if (cache) { cache.set(obj, newObj); } return newObj; }

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var LOCALE_KEY_NAME;
exports.LOCALE_KEY_NAME = LOCALE_KEY_NAME;

(function (LOCALE_KEY_NAME) {
  LOCALE_KEY_NAME["region"] = "region";
  LOCALE_KEY_NAME["lang"] = "lang";
  LOCALE_KEY_NAME["currency"] = "currency";
  LOCALE_KEY_NAME["tz"] = "tz";
})(LOCALE_KEY_NAME || (exports.LOCALE_KEY_NAME = LOCALE_KEY_NAME = {}));

var Locale = /*#__PURE__*/function () {
  function Locale() {
    var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

    _classCallCheck(this, Locale);

    _defineProperty(this, "key", void 0);

    _defineProperty(this, "maxAge", void 0);

    _defineProperty(this, "domain", void 0);

    _defineProperty(this, "path", void 0);

    _defineProperty(this, "container", void 0);

    this.key = options.key || '';
    this.maxAge = options.maxAge;
    this.domain = options.domain;
    this.path = options.path;
    this.container = Util.getContainerType();
  }

  _createClass(Locale, [{
    key: "getKey",
    value: function getKey() {
      return this.key;
    }
  }, {
    key: "getPureLocale",
    value: function getPureLocale() {
      var localeObject = this[this.container] ? this[this.container]() : "";
      return localeObject;
    }
    /**
     * get locale object = > {regin: 'CN', lang: 'zh-CN', currency: 'CNY', tz: 'Asia/Shanghai'}
     * @param options 
     */

  }, {
    key: "getLocale",
    value: function getLocale(options) {
      var localeObject = this.getPureLocale();

      if (options && options.short && localeObject[LOCALE_KEY_NAME.lang]) {
        var shortLang = localeObject[LOCALE_KEY_NAME.lang].split('-')[0];
        localeObject[LOCALE_KEY_NAME.lang] = shortLang;
      } // for compatible common-info:  http://gitlab.alibaba-inc.com/lzdmod/common-info


      if (localeObject.region) localeObject.regionID = localeObject.region;
      if (localeObject.lang) localeObject.language = localeObject.lang;
      return localeObject;
    }
    /**
     * get locale object for compatible common-info:  http://gitlab.alibaba-inc.com/lzdmod/common-info
     * @param options {short: true/false}
     */

  }, {
    key: "getEnv",
    value: function getEnv(options) {
      var op = Object.assign({}, {
        short: true
      }, options);
      var localeObject = this.getLocale(op);
      return localeObject;
    }
    /**
     * get locale string = > regin=CN&lang=zh-CN&currency=CNY&tz=Asia/Shanghai
     * @param options 
     */

  }, {
    key: "getLocaleString",
    value: function getLocaleString() {
      var locale = this.getPureLocale();
      return Util.stringifyToQueryString(locale);
    }
    /**
     * set locale use key and value
     * @param key the key of locale id. by default: region represent countries, dependent territories, and special areas of geographical interest , lang represent language tag , currency represent currency , tz represent time zone
     * @param value the value, in specification, region use ISO 3166-1 alpha-2 , lang use BCP-47 , currency use ISO 4217  , tz use IANA timezone code
     */

  }, {
    key: "setLocale",
    value: function setLocale(key, value) {
      if (!key) return;

      var object = _defineProperty({}, key, value);

      this.setLocaleWithObject(object);
    }
    /**
     * set region
     * @param code region code , use ISO 3166-1 alpha-2 
     */

  }, {
    key: "setRegion",
    value: function setRegion(code) {
      if (!code) return;
      this.setLocale(LOCALE_KEY_NAME.region, code);
    }
    /**
     * set language
     * @param code language tag , use BCP-47
     */

  }, {
    key: "setLang",
    value: function setLang(code) {
      if (!code) return;
      this.setLocale(LOCALE_KEY_NAME.lang, code);
    }
    /**
     * set currency code 
     * @param code currency code , use ISO 4217
     */

  }, {
    key: "setCurrency",
    value: function setCurrency(code) {
      if (!code) return;
      this.setLocale(LOCALE_KEY_NAME.currency, code);
    }
    /**
     * set time zone
     * @param code set time zone, use iana code 
     */

  }, {
    key: "setTimeZone",
    value: function setTimeZone(code) {
      if (!code) return;
      this.setLocale(LOCALE_KEY_NAME.tz, code);
    }
  }, {
    key: "setLocaleWithObject",
    value: function setLocaleWithObject(object) {
      if (this.container === Util.ContainerType.weex) return;
      var currentLocale = this.getLocale();
      var mergedLocale = Object.assign({}, currentLocale, object);
      var strValue = Util.stringifyToQueryString(mergedLocale);
      Cookie.set(this.key, strValue, {
        path: this.path || '/',
        maxAge: this.maxAge,
        // to second
        domain: this.domain
      });
    }
  }, {
    key: "weex",
    value: function weex() {
      var str = "";

      if ((typeof WXLocaleironment === "undefined" ? "undefined" : _typeof(WXLocaleironment)) === "object") {
        str = WXLocaleironment && WXLocaleironment[this.key] || ""; // eslint-disable-line
      }

      var localeObject = Util.parseQueryString(str);
      return localeObject;
    }
  }, {
    key: "web",
    value: function web() {
      var str = "";
      var localeObject = {}; // try to get from url query

      if (location && location.search) {
        // eslint-disable-line 
        var search = location.search.replace(/^[?#&]/, ''); // eslint-disable-line

        var keysAndValue = Util.parseQueryString(search);
        var keyNames = Util.objectValues(LOCALE_KEY_NAME);
        Object.keys(keysAndValue).forEach(function (key) {
          if (keyNames.indexOf(key) >= 0
          /* &&keysAndValue[key] */
          ) {
            localeObject[key] = keysAndValue[key];
          }
        });
      } // try to get from _i18n_ inject by app: WindVane


      if ((typeof window === "undefined" ? "undefined" : _typeof(window)) === "object") {
        var win = window;
        str = window[this.key]; // windows['x-hng']

        if (str) {
          var windowLocale = Util.parseQueryString(str);
          localeObject = Object.assign({}, windowLocale, localeObject);
        } else if (_typeof(win.g_config) === 'object') {
          var _win$g_config = win.g_config,
              regionID = _win$g_config.regionID,
              language = _win$g_config.language; // read from window.g_config

          var localeFromGConfig = {};
          if (regionID) localeFromGConfig[LOCALE_KEY_NAME.region] = regionID;
          if (language) localeFromGConfig[LOCALE_KEY_NAME.lang] = language;
          localeObject = Object.assign({}, localeFromGConfig, localeObject);
        }
      } // try to get from cookie


      if (!str) {
        var cookieLocale = {};
        str = Cookie.get(this.key); // x-hng 

        if (!str) {
          // if has not x-hng ,read from hng
          var hng = Cookie.get('hng');

          if (hng) {
            var hngArray = hng.split('|');
            if (hngArray[0]) cookieLocale[LOCALE_KEY_NAME.region] = hngArray[0]; // eslint-disable-line

            if (hngArray[1]) cookieLocale[LOCALE_KEY_NAME.lang] = hngArray[1]; // eslint-disable-line

            if (hngArray[2]) cookieLocale[LOCALE_KEY_NAME.currency] = hngArray[2]; // eslint-disable-line
          }
        } else {
          cookieLocale = Util.parseQueryString(str);
        }

        localeObject = Object.assign({}, cookieLocale, localeObject);
      }

      return localeObject;
    }
  }, {
    key: "clear",
    value: function clear() {
      Cookie.remove(this.key);
    }
  }]);

  return Locale;
}();

var _default = Locale;
exports.default = _default;
//# sourceMappingURL=locale.js.map