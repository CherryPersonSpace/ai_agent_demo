"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ContainerType = void 0;
exports.decode = decode;
exports.encode = encode;
exports.getContainerType = getContainerType;
exports.objectValues = objectValues;
exports.parseQueryString = parseQueryString;
exports.splitOnFirst = splitOnFirst;
exports.stringifyToQueryString = stringifyToQueryString;

function _slicedToArray(arr, i) { return _arrayWithHoles(arr) || _iterableToArrayLimit(arr, i) || _unsupportedIterableToArray(arr, i) || _nonIterableRest(); }

function _nonIterableRest() { throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _iterableToArrayLimit(arr, i) { var _i = arr == null ? null : typeof Symbol !== "undefined" && arr[Symbol.iterator] || arr["@@iterator"]; if (_i == null) return; var _arr = []; var _n = true; var _d = false; var _s, _e; try { for (_i = _i.call(arr); !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"] != null) _i["return"](); } finally { if (_d) throw _e; } } return _arr; }

function _arrayWithHoles(arr) { if (Array.isArray(arr)) return arr; }

function _typeof(obj) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (obj) { return typeof obj; } : function (obj) { return obj && "function" == typeof Symbol && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; }, _typeof(obj); }

var ContainerType;
exports.ContainerType = ContainerType;

(function (ContainerType) {
  ContainerType["weex"] = "weex";
  ContainerType["web"] = "web";
})(ContainerType || (exports.ContainerType = ContainerType = {}));

function getContainerType() {
  var type = "";

  if (typeof callNative === "function") {
    // eslint-disable-line
    type = ContainerType.weex;
  } else if ((typeof navigator === "undefined" ? "undefined" : _typeof(navigator)) === "object" && (navigator.appCodeName === "Mozilla" || navigator.product === "Gecko")) {
    type = ContainerType.web;
  }

  return type;
}

function splitOnFirst(str, separator) {
  if (separator === "") {
    return [str];
  }

  var separatorIndex = str.indexOf(separator);

  if (separatorIndex === -1) {
    return [str];
  }

  return [str.slice(0, separatorIndex), str.slice(separatorIndex + separator.length)];
}

function decode(str) {
  try {
    return decodeURIComponent(str);
  } catch (error) {
    return str;
  }
}

function encode(str) {
  try {
    return encodeURIComponent(str);
  } catch (error) {
    return str;
  }
}

function parseQueryString(str) {
  if (!str) return {};
  var keysAndValue = str.split("&");
  var localeObject = {};
  keysAndValue.forEach(function (el) {
    var _splitOnFirst = splitOnFirst(el.replace(/\+/g, " "), "="),
        _splitOnFirst2 = _slicedToArray(_splitOnFirst, 2),
        key = _splitOnFirst2[0],
        value = _splitOnFirst2[1];

    var decodeValue = value === undefined ? "" : decode(value);
    localeObject[key] = decodeValue;
  });
  return localeObject;
}

function stringifyToQueryString(object) {
  var keys = Object.keys(object);
  var str = keys.map(function (key) {
    var value = object[key];

    if (value === undefined) {
      return "";
    }

    if (value === null) {
      return encode(key);
    }

    var itemStr = "".concat(encode(key), "=").concat(encode(value));
    return itemStr;
  }).filter(function (x) {
    return x.length > 0;
  }).join("&");
  return str;
}

function objectValues(obj) {
  var vals = [];

  for (var _key in obj) {
    // eslint-disable-line
    if (Object.prototype.hasOwnProperty.call(obj, _key)) {
      vals.push(obj[_key]);
    }
  }

  return vals;
}
//# sourceMappingURL=util.js.map