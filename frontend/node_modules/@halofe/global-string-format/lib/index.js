"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
exports.format = format;
exports.init = init;
exports.sdkVersion = void 0;

var _universalEnv = require("universal-env");

var _stringFormat = _interopRequireDefault(require("./string-format"));

var _util = require("./util");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// eslint-disable-line
var NAMESPACE = '__GLOBAL_STRING_FORMAT__'; // for weex

var INSTANCES_CACHE = {}; // for SSR, this should be one-time context

var DISPOSABLES__CACHE;

function getInstances(context) {
  if (context) {
    DISPOSABLES__CACHE = context;
    return context;
  }

  if (DISPOSABLES__CACHE) {
    return DISPOSABLES__CACHE;
  }

  if (_universalEnv.isWeex) {
    // in weex use instances
    return INSTANCES_CACHE;
  }

  var g = typeof global !== 'undefined' ? global : window;

  if (!g[NAMESPACE]) {
    g[NAMESPACE] = {};
  }

  return g[NAMESPACE];
}

function init(locale, strings) {
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  var _options$name = options.name,
      name = _options$name === void 0 ? 'default' : _options$name,
      context = options.context;
  var instances = getInstances(context);
  var stringFormat = new _stringFormat.default(locale, strings);
  instances[name] = stringFormat;
  return stringFormat;
}

function format(des, variable) {
  var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
  var _options$name2 = options.name,
      name = _options$name2 === void 0 ? 'default' : _options$name2;
  var instances = getInstances();
  var instance = instances[name];

  if (!instance) {
    (0, _util.error)("\n    Not found instance named ".concat(name, ",\n    make sure to call formatString.format() after formatString.init()\n    "));
    return des.defaultMessage || des.id || '';
  }

  var string = instance.format(des, variable, options);
  return string;
}

var _default = {
  init: init,
  format: format,
  StringFormat: _stringFormat.default
};
exports.default = _default;
var sdkVersion = "1.0.8";
exports.sdkVersion = sdkVersion;
//# sourceMappingURL=index.js.map