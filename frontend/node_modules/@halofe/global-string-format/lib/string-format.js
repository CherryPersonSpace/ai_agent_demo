"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _intlMessageformat = _interopRequireDefault(require("intl-messageformat"));

var _util = require("./util");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } }

function _createClass(Constructor, protoProps, staticProps) { if (protoProps) _defineProperties(Constructor.prototype, protoProps); if (staticProps) _defineProperties(Constructor, staticProps); Object.defineProperty(Constructor, "prototype", { writable: false }); return Constructor; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

var PSEUDO_LANGUAGE_TAG = 'pd-KV';

// 格式支持：en-US ， en_US,  en_us
function formatLocale() {
  var locale = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : '';

  if (typeof locale !== 'string') {
    (0, _util.error)('"locale" is not a string, please check your input locale');
    locale = '';
  }

  var result = locale.toLowerCase();
  result = result.replace('-', '_');
  return result;
}

function truncationCompare(locale, superior) {
  if (locale === superior) {
    return true;
  }

  var localeMatch = locale.match(/(\S*)_/);

  if (localeMatch) {
    var localTruncation = localeMatch[1];
    return truncationCompare(localTruncation, superior);
  }

  return false;
}

function isSuperior(locale, superior) {
  // en-us , en_us, En-us等为en的上级
  var localeFormat = formatLocale(locale);
  var superiorFormat = formatLocale(superior);

  if (localeFormat === superiorFormat) {
    return true;
  } // 对localeFormat进行截断后判断


  return truncationCompare(localeFormat, superiorFormat);
}

var StringFormat = /*#__PURE__*/function () {
  // dictionary = {
  //   'eee.ddd': {
  //     format: function (): string{}
  //   },
  //   'address.address_book.title': {
  //     format: function (): string{}
  //   }
  // }

  /**
   *  @param locale Languages that need to be localized
   *  @param strings Data dictionary, ICU grammar
   */
  function StringFormat(locale, strings) {
    var _this = this;

    _classCallCheck(this, StringFormat);

    _defineProperty(this, "dictionary", void 0);

    _defineProperty(this, "strings", void 0);

    _defineProperty(this, "locale", void 0);

    _defineProperty(this, "medusa", void 0);

    this.dictionary = {}; // 格式支持：en-US ， en_US,  en_us

    this.locale = locale || ''; // strings[locale] = {
    //   'eee.ddd': '我有 ${count}  条文案',
    //   'address.address_book.title': '我的地址是',
    // }

    if (!strings) {
      (0, _util.warn)("param \"strings\" is undefined, please check your incoming");
      strings = {};
    }

    if (strings.medusa) {
      this.medusa = strings.medusa;
      delete strings.medusa;
    }

    this.strings = strings;
    var localeStrings = strings[locale];

    if (!localeStrings) {
      // 因en_us en-us en-Us等格式不同没找到对应文案的，转换格式后再找
      Object.keys(strings).forEach(function (language) {
        if (formatLocale(language) === formatLocale(locale)) {
          (0, _util.warn)("no string list for locale: ".concat(locale, " , will replace it with ").concat(language));
          localeStrings = strings[language];
        }
      }); // 因en_us en-us en-Us可找到en上级

      if (!localeStrings) {
        Object.keys(strings).forEach(function (language) {
          if (isSuperior(locale, language)) {
            (0, _util.warn)("no value for locale: ".concat(locale, " , will replace it with").concat(language));
            localeStrings = strings[language];
          }
        });
      }
    }

    if (!localeStrings || localeStrings && Object.keys(localeStrings).length === 0) {
      (0, _util.warn)("no value for locale: ".concat(locale, ", please check your second param"));
      localeStrings = {};
    }

    Object.keys(localeStrings).forEach(function (id) {
      try {
        var formatter = new _intlMessageformat.default(localeStrings[id], locale);
        _this.dictionary[id] = formatter;
      } catch (e) {
        _this.dictionary[id] = {};
        (0, _util.error)(e);
      }
    });
  }
  /**
   * 获取 strings
   */


  _createClass(StringFormat, [{
    key: "getStrings",
    value: function getStrings() {
      return this.strings;
    }
  }, {
    key: "formatString",
    value: function formatString(formatter, variables, id) {
      var medusa = this.medusa,
          locale = this.locale;
      var formatedString = formatter.format(variables);

      if (medusa && medusa.app_name && locale === PSEUDO_LANGUAGE_TAG) {
        formatedString = "##@@@".concat(id, "##").concat(medusa.app_name, "@@@##").concat(formatedString);
      }

      return formatedString;
    }
    /**
     * Produce the formatted string for displaying to the user
     * @param des {id: string, defaultString: string }
     * @param variables {[propName: string]: any}
     * @param options {FormatOptions}
     */

  }, {
    key: "format",
    value: function format() {
      var des = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {
        id: ''
      };
      var variables = arguments.length > 1 ? arguments[1] : undefined;
      var options = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : {};
      var defaultString = des.defaultMessage || des.defaultString || '';
      var hasVariables = variables && Object.keys(variables).length > 0;
      var tokenDelimiter = '';
      var tokenizedVariables = {};
      var elements = {};
      var React = options.React,
          component = options.component,
          _options$tagName = options.tagName,
          tagName = _options$tagName === void 0 ? 'span' : _options$tagName; // reader react nodes

      if (hasVariables) {
        // Creates a token with a random UID that should not be guessable or
        // conflict with other parts of the `message` string.
        var uid = Math.floor(Math.random() * 0x10000000000).toString(16);

        var generateToken = function () {
          var counter = 0;
          return function () {
            counter += 1;
            return "ELEMENT-".concat(uid, "-").concat(counter);
          };
        }(); // Splitting with a delimiter to support IE8. When using a regex
        // with a capture group IE8 does not include the capture group in
        // the resulting array.


        tokenDelimiter = "@__".concat(uid, "__@"); // Iterates over the `props` to keep track of any React Element
        // values so they can be represented by the `token` as a placeholder
        // when the `message` is formatted. This allows the formatted
        // message to then be broken-up into parts with references to the
        // React Elements inserted back in.

        Object.keys(variables).forEach(function (name) {
          var variable = variables[name];

          if (React && React.isValidElement(variable)) {
            var token = generateToken();
            tokenizedVariables[name] = tokenDelimiter + token + tokenDelimiter;
            elements[token] = variable;
          } else {
            tokenizedVariables[name] = variable;
          }
        });
      }

      var formattedMessage;

      if (!this.dictionary[des.id]) {
        if (typeof des === 'string') {
          (0, _util.error)("type of  \"des\" must be object, like:  {id: '', defaultString: ''}, instead of string ");
          formattedMessage = des || '';
        } else {
          (0, _util.warn)("there is no string with id=\"".concat(des.id, "\" for locale: \"").concat(this.locale, "\", use defaultString or id as fallback"));
          var message = defaultString || des.id || '';

          try {
            var formatter = new _intlMessageformat.default(message, this.locale);
            formattedMessage = this.formatString(formatter, tokenizedVariables, des.id);
          } catch (e) {
            formattedMessage = message;
          }
        }
      } else {
        try {
          formattedMessage = this.formatString(this.dictionary[des.id], tokenizedVariables, des.id);
        } catch (e) {
          var desId = des.id ? ', using id as fallback.' : '';

          var _message = defaultString ? ', using defaultString as fallback.' : desId;

          (0, _util.error)("format string: ".concat(des.id, " for locale  ").concat(this.locale, " ").concat(_message, " \n").concat(e));
          formattedMessage = defaultString || des.id || '';
        }
      }

      var hasElements = elements && Object.keys(elements).length > 0;

      if (hasElements) {
        // Split the message into parts so the React Element values captured
        // above can be inserted back into the rendered message. This
        // approach allows messages to render with React Elements while
        // keeping React's virtual diffing working properly.
        var nodes = formattedMessage.split(tokenDelimiter).filter(function (part) {
          return !!part;
        }).map(function (part) {
          return elements[part] || part;
        });
        var comp = component || tagName; // Needs to use `createElement()` instead of JSX, otherwise React will
        // warn about a missing `key` prop with rich-text message formatting.

        return React ? React.createElement.apply(React, [comp, null].concat(_toConsumableArray(nodes))) : nodes;
      }

      return formattedMessage;
    }
  }]);

  return StringFormat;
}();

exports.default = StringFormat;
//# sourceMappingURL=string-format.js.map