# Core
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0

# AgentScope 2.0.4.post1 (core framework with openai / httpx / sse dependencies)
agentscope==2.0.4.post1

# AG-UI protocol (SSE event encoding/decoding)
ag-ui-protocol>=0.1.19

# RAG: PostgreSQL vector search + Ollama local Embedding
psycopg[binary]>=3.1
ollama>=0.3
python-docx>=1.1

# Document parsing
openpyxl>=3.1           # .xlsx Excel
python-pptx>=0.6        # .pptx PowerPoint
PyMuPDF>=1.24           # .pdf PDF (import fitz)
xlrd>=2.0               # .xls legacy Excel

# Tools
zhdate>=0.1             # Chinese lunar calendar (lunar date conversion)

# Frontend uses vanilla JS, no extra Python frontend dependencies needed
